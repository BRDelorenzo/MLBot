[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Cobertura mínima focada em auth, multi-tenancy, publish, crypto.
# Rode: pytest --cov=app --cov-fail-under=60
addopts = --strict-markers
filterwarnings =
    ignore::DeprecationWarning
